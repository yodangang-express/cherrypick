apiVersion: v1
kind: Service
metadata:
  name: cherrypick
  namespace: cherrypick
spec:
  selector:
    name: cherrypick
  ports:
    - protocol: TCP
      name: http
      port: 80
      targetPort: 3000
    - protocol: TCP
      name: https
      port: 443
      targetPort: 3000
